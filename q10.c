#include <stdio.h>

int main()
{
    int n,i,j,k=0,f;
    printf("Enter the length of the array : "); scanf ("%d",&n);
    
    int arr[n], freq[1000000];
    for (i=0;i<n;i++)
    {
        f=0;
        for (j=0;j<n;j++)
        {
            if (arr[i]==arr[j])   
            ;         
        }
    }

    
    
    return 0;
}